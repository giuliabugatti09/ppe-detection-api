[pytest]
# Garante que a raiz do projeto seja adicionada ao caminho de busca
# de módulos do Python durante os testes. Sem isso, "from app.main
# import app" falha com ModuleNotFoundError, porque o pytest não
# adiciona a raiz do projeto ao sys.path automaticamente.
pythonpath = .

testpaths = tests